{% extends 'base.html.twig' %}



{% block body %}
<div class="container">                

    <div class="col-md-8 col-md-offset-2">
        <div class="panel panel-default">
          <div class="panel-heading">Dodavanje dovosti</div>
          <div class="panel-body">
            {{ form_start(form) }}
            <div class="form_group">
                <div class="col-md-8 col-md-offset-2">
                  {{ form_row(form.name, {label:'Naslov novosti','attr': {'class': 'form-control'}}) }}
                </div>
            </div> 
            <div class="form_group">
                <div class="col-md-8 col-md-offset-2">
                  {{ form_row(form.sadrzaj, {label: 'Sadržaj' ,'attr': {'class': 'form-control'}}) }}
                </div>
            </div>            

            <div class="form-group">
                <div class="col-md-8 col-md-offset-2" style="margin-top:5px;">
                    <button type="submit" class="btn btn-primary">
                        <i class="fa fa-btn"></i> Dodaj novost
                    </button>
                </div>
            </div>
            {{ form_end(form) }}
          </div>
        </div>
    </div>
  </div>
</div>
{% endblock %}