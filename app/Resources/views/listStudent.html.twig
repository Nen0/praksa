{% extends 'base.html.twig' %}

{% block body %}

<div class="container">
   <div class="row">
      <div class="col-md-10 col-md-offset-1">
         <div class="panel panel-success">
            <div class="panel-heading text-center">Profesori</div>
            <table class="table">
               <thead>
                  <tr>
                     <th>Id</th>
                     <th>Ime</th>
                     <th>Actions</th>
                  </tr>
               </thead>
               <tbody>
                  {% for student in studenti %}
                  <tr>
                     <td>{{ student.id }}</a></td>
                     <td>{{ student.name }}</td>
                     <td>
                        <ul class="list-inline">
                           <li class="list-inline-item">                              
                              <a href="{{ path('editStudent', { 'id': student.id }) }}">Uredi</a>
                           </li>
                           <li class="list-inline-item">                              
                              <a href="{{ path('deleteStudent', { 'id': student.id }) }}">Delete</a>
                           </li>                                                     
                        </ul>
                     </td>
                  </tr>
                  {% endfor %}
               </tbody>
            </table>
         </div>
      </div>
   </div>
</div>


    
{% endblock %}
